"""Implementation package for the cow_worktree CoW-worktree wrapper.

`bin/cow_worktree.py` is the stable entrypoint; it re-exports this
package's public API for backward-compatible white-box test access. See
references/design.md for the algorithm and threat model.
"""
